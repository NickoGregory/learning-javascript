// 1. Declare a variable called `greeting` and assign it the value
//  `"Hello, world!"`. Log it to the console. 

// 2. Create an array `colors` with the values `"red"`, `"green"`, and 
// `"blue"`. Add `"yellow"` to the array using `push()`.  

// 3. Write a function `multiply` that takes two parameters, `a` and `b`,
//  and returns their product. Call this function with `3` and `4`, and 
// log the result to the console.  

// 4. Write a for loop that logs the numbers 0 to 5.

// 5. Create a variable `message` and assign it the concatenated value of
//  `"Good "` and `"morning"`. Log it to the console.

// 6. Declare an empty array `items` and add three values to it using 
// `push()`.  

// 7. Write a `for` loop that iterates over the `colors` array and logs
//  each color.

// 8. Write a function `padRow` that takes a string parameter and returns
//  the string repeated three times. 
function padRow(inputString) {
    return inputString.repeat(3);
}

// Example usage
let result = padRow("Hello ");
console.log(result);
